# SPEC v6.1 — Player-Centric R-NaD + NeuRD (заморожено: 2026-09-19)

Наследует v6.0 IRONCLAD с фиксами аудита от 2026-09-19. Изменения против v6.0:
(S1) по-акционный регуляризатор в силе NeuRD (порт `eta_log_policy` референса);
(S2) NaN-безопасные маски `torch.where` вместо умножения; (S3) контракты батча
проверяются в рантайме; (S4) канонизация: «наименьшие неиспользованные индексы
в порядке первого появления»; (S5) TD части политики — с чистой наградой
(регуляризованная — только у критика), как в референсе.

## 1. Область
- Фаза 1: 2 игрока, zero-sum, эпизодические игры. Интерфейсы рассчитаны на N игроков.
- Движок не знает покер. Вход: `TrajectoryBatch`. Покерная специфика — в `games/`, `cards/`.

## 2. Интерфейс игры (`kalay/games/base.py`)
`GameEnv`: `n_players, n_actions(K), obs_dim`; `clone(); is_chance(); chance_probs();
step_chance(i); current_player(); legal_actions_mask()->[K]; step(a); is_terminal();
rewards()->[n_players]; obs(player)->[obs_dim]`. Наблюдение игрока не содержит
чужих скрытых карт. `reset_env(env, rng)` разыгрывает все chance-узлы.

## 3. Канонизация карт (`kalay/cards/canonical.py`)
Рука сортируется по (−ранг, −сырой индекс масти); h1→масть 0; h2→0 (одномастные)
или 1 (разномастные); масти руки LOCKED до конца раздачи. Карты борда: совпадающие
с мастью руки → 0/1; новые масти → **наименьшие неиспользованные индексы** в порядке
первого появления (для suited это 1,2,3). Для карманных пар: после канонизации борда
проверяется перестановка 0↔1 в бордовых картах, применяется лексикографически
меньшая. Инварианты (tier A): полнота орбиты (24 перестановки), отсутствие ложных
слияний, префикс-стабильность между улицами (append-only KV).

## 4. Контракты батча (`TrajectoryBatch.validate`, tier A)
- C1: `obs[T,B,obs_dim]`, `legal_mask[T,B,K]`, `action_taken/hand_lengths/behavior_prob
  /terminal_reward [T,B]`, `player_id[B]`; конечность всех тензоров.
- C2: `1 ≤ hand_lengths[b] ≤ T` (раздачи без решений отбрасываются коллектором).
- C3: взятое действие легально на всех реальных шагах: `mask.gather(-1, a) == 1`.
- C4: `0 < behavior_prob ≤ 1+1e-6` на реальных шагах.
- C5: терминальная награда ненулевая **ровно на** `t = hand_lengths−1` (сумма по
  столбцу равна значению в последней реальной строке).
- C6: `0 ≤ player_id < n_players`.

## 5. Движок (`kalay/core`)
### 5.1 Распределения
`pi = softmax(where(mask, z, −1e9))`; то же для якорной `pi_ref`. Нелегальные — 0.

### 5.2 Регуляризованная награда критика
`r_reg = −tau · KL(pi‖pi_ref) + r_norm`, `r_norm = terminal_reward / reward_scale`.
KL — скаляр по инфосету (роль `eta_reg_entropy` референса). Входит **только** в
V-таргеты критика.

### 5.3 V-trace ([T,B], обратная рекурсия)
`rho = min(rho_clip, pi_taken/mu)`, `c = min(c_clip, pi_taken/mu)` (по умолчанию
обе 1.0; `rho_clip` конфигурируем — референс использует ∞).
```
next_V  = where(t+1 < L, v_preds[t+1], 0)
next_vt = where(t+1 < L, v_targets[t+1], 0)
delta_t = rho_t · (r_reg_t + γ·next_V − v_preds_t)
v_targets_t = v_preds_t + delta_t + γ·c_t · where(t < L−1, next_vt − next_V, 0)
```
Все маски — `torch.where` (S2): NaN в паддинговых строках не проникает в реальные.
`L_v = 0.5 · Σ where(decision, (v − v_targets)², 0) / max(Σ decision, 1)`.

### 5.4 NeuRD: по-акционный регуляризатор (S1, главный фикс v6.1)
```
log_ratio[a] = log pi(a) − log pi_ref(a)                      # по действиям
td = r_norm + γ · where(t+1 < L, v_targets[t+1], 0) − v_preds # ЧИСТАЯ награда (S5)
q_vr[a] = v_preds + tau·(log pi_ref[a] − log pi[a])           # регуляризатор
q_vr[a_t] += td / max(mu, mu_floor)                           # TD только на взятом
q_vr *= legal_mask
adv[a] = q_vr[a] − Σ_b pi[b]·q_vr[b]                          # контрфакторное
adv = clamp(adv, ±A_MAX).detach()
```
### 5.5 Силы и policy loss
Центрирование — по легальным (фикс #1156): `mean_z = Σ z·m / Σ m`; `z_c = z − mean_z`
(дифференцируемое среднее — сдвиг-инвариантность лосса).
```
can_decrease = z_c > −Z;  can_increase = z_c < +Z
force = can_decrease·min(adv,0) + can_increase·max(adv,0)
L_pi = − Σ legal · z_c · sg(force) / max(Σ decision, 1)
```
Сила прикладывается ко **всем легальным** логитам (как в референсе), не только к взятому.

### 5.6 Шаг обучения, якорь и оптимизм
Value-шаг → policy-шаг; клип градиентов 1.0; AdamW (backbone wd=1e-2, head wd=0).
**Якорная фаза** (режим `drift`, дефолт выведенного конфига) заканчивается по
трёхстороннему триггеру — дисциплина двух масштабов (внутренний цикл должен
отработать до сдвига центра):
1. накопленный KL(π‖π_ref) ≥ `anchor_cum_kl` (3 ната) — «унесло» (циклические игры);
2. settle: |EMA_fast(0.9) − EMA_slow(0.99)| ≤ 5%·EMA_slow при длине фазы ≥ 300 шагов
   — скорость дрейфа остыла, внутренний цикл отработал (сходящиеся игры);
3. hard cap 4·anchor_period.
Эмпирика: чистый drift-триггер (якорь по накопленному KL) держал Kuhn на nc≈0.18
против 0.014 при каденсном режиме — ре-якорение в процессе обучения разрушает прогресс.
Якорь — жёсткая копия (EMA-смешивание α из референса — отложенное упрощение).
τ — константа класса (см. §6): уточнение до Нэша даёт Nash-усреднение по якорным фазам,
а не малый τ; аннейлинг к τ→0 разрушал диссипацию (измерено на обеих играх).
**Оптимизм** (`optimism_beta`, дефолт 0 — выкл): экстраполяция градиента политики
`g ← g + β(g_t − g_{t−1})` — гашение гамильтоновой ротации первого порядка. Включается
только через гейт-эксперимент (фиксированный бюджет, сиды 0-2, метрика nc_avg).

### 5.6a Метрики здоровья
nc_cur осциллирует с постоянной амплитудой (нет экспоненциального спада при
фиксированном якоре) — lr велик относительно τ. rail_fraction > 0 стабильно —
проверить масштаб наград/mu_floor. force_gated_fraction высокий — мёртвый градиент
центрирования (см. фиксы #1156).

## 6. Конфиг: выводится, а не подбирается (`kalay/core/gamestats.py`)
Всё игровое выводится: T и reward_scale — точным DFS-перечислением; mu_floor —
замкнутой формулой π_min(K, Z) = e^(−Z)/((K−1)e^Z + e^(−Z)); якорный каденс —
онлайн-триггером §5.6. Константы КЛАССА (единые для всех игр класса, калиброваны
один раз на игрушках, сиды 0-2 в tier B — защита от переобучения констант):
`tau0 = tau_min = 0.3` (постоянный регуляризатор), `z_thresh = 3.0`,
`lr_policy = lr_value = 1e-3`, `anchor_cum_kl = 3.0`. Единственная потенциальная
перекалибровка при переходе класса (limit/no-limit) — lr. Гиперпараметры НЕ входят
в заморозку (тюнятся под прохождение tier B); пороги tier B — входят.

## 7. Осознанные отклонения от референса OpenSpiel `rnad.py`
| Аспект | Референс | v6.1 | Статус |
|---|---|---|---|
| Регуляризатор в силе | `eta_log_policy` по-акционно | то же (S1) | совпадает |
| KL-скаляр в награде критика | `eta_reg_entropy` | то же | совпадает |
| ρ | ∞ | 1.0 (конфиг) | упрощение, задокументировано |
| Якорь | EMA + α-смешивание 2 снапшотов | жёсткая копия | упрощение Фазы 1 |
| clip силы | 10 000 | 100 | игрушки, награды ∈ [−1,1] |
| Z | 2.0 | 3.0 | выбор v6.x |
| Акторящий порог 3% | есть | нет (mu_floor = π_min(K,Z), выводится) | вернуть перед HUNL |

## 8. Пререгистрация tier B (пороги заморожены ДО первого запуска)
- **RPS:** якорно-средняя политика; NashConv ≤ **0.05** (единицы платежа [−1,1]);
  бюджет **4000** learner-шагов × батч **128** раздач; сиды **{0,1,2}**.
  Эталон: Нэш равномерный, NashConv(Нэш) = 0.
- **Kuhn:** NashConv ≤ **0.05** фишек (макс свинг ±2); бюджет **15000** × **128**;
  сиды **{0,1,2}**. Эталон: значение игры −1/18 (аналитика).
- **Leduc:** якорно-средняя политика; NashConv ≤ **0.15** фишек и одновременно
  ≤ 10% × NashConv(uniform-политики) — второй якорь страхует единицы; бюджет
  **150000** шагов × **128** раздач; сиды **{0,1,2}**. Внешние ориентиры масштаба:
  табличный CFR на Leduc сходится к ~0.0014 фишек; глубокие безмодельные методы
  NFSP-эпохи — порядка сотых-десятых фишки. Определение игры — §9.
- **Push-Fold 10bb:** NashConv ≤ **0.15** bb/руку И prior-weighted TV диапазонов
  с точным Нэшем ≤ **0.05** (пуш-диапазон BTN и колл-диапазон BB по отдельности);
  бюджет-потолок **100000** × **128**, сиды **{0,1,2}**; чекпоинт-семантика:
  оценка каждые 10000 шагов, зачёт при выполнении обоих условий на чекпоинте
  и подтверждении на следующем. Эталон — собственный точный солвер: таблица
  эквити 169×169 (1M симуляций/матчап, сид 20260920, SE ≈ 0.01 bb) + CFR+;
  контроль: NashConv(Нэш) = 0, value BTN = −0.0454 bb/руку, BTN пушит 58.3%,
  BB колит 37.4%. Порог прегистрирован ДО обучения агента.
- Порог последующих игр (HULHE-toy, HUNL-toy) прегистрируется в этом файле
  ДО реализации соответствующего адаптера.
- **8.1 ЧЕРНОВИК: ревизия метрики push-fold — ТРЕБУЕТСЯ РЕШЕНИЕ ВЛАДЕЛЬЦА.**
  Факты: nc зелёный с 8k при всех τ (0.012-0.04 при пороге 0.15); TV пуш-диапазона
  выходит на полку ~0.085 (τ=0.03: 0.113@8k → 0.0848@32k, наклон ~0.0003/8k);
  τ на полку почти не влияет. Структура (диагностика 20k/τ=0.03): 98.3% TV-массы
  на ЧИСТЫХ по Нэшу руках, где агент держит ~0.4-0.6 (76o: агент 0.23 против
  1.0; Q6o: 0.64 против 0.0); на смешанных руках Нэша — 1.7%; якорное среднее
  == текущей политике. Механизм: околопороговые руки имеют плоский EV-ландшафт
  (силы NeuRD около нуля), размытые self-play диапазоны самоподдерживаются.
  Прецедент: глубокие безмодельные агенты в литературе репортят эксплуатируемость
  (bb/hand), TV-0.05 для нейроагента прецедента не имеет. Предложение: TV —
  репорт-диагностика; gate = NashConv ≤ 0.15 bb (уже выполняется) И TV ≤ 0.15
  (двойной запас от измеренной полки). Альтернатива: полный прогон потолка 100k
  на τ=0.03 и фиксация достижимой TV как порога. СТАТУС: НЕ ПРИМЕНЕНО.
- Ограничение истории (внешний аудит I1): RPS/Kuhn пороги существуют только
  в корневом коммите f737303 вместе с результатами — git не доказывает
  «порог раньше обучения». С Leduc/PF порядок раздельными коммитами соблюдён;
  для следующих игр — только так.

## 9. Дорожная карта игр и определения
`rps` (ядро NeuRD, без инфосетов) → `kuhn` (инфосеты, чередование) → `leduc` →
`pushfold` (канонизация на полном диапазоне) → `hulhe_toy` → `hunl_toy`
(1–2 улицы, дискретные сайзинги) → HUNL.

**Push-Fold NLHE 10bb (замороженное определение):** стеки по 10bb, блайнды
0.5/1, без анте. SB действует первым: 0=фолд, 1=пуш на 10bb. BB против пуша:
0=фолд, 1=колл. K=2; T=1. Терминалы: фолд SB ±0.5; фолд BB ±1; колл — шоудаун
5 бордов, ±10. Наблюдение — S4-канонизированная карманная пара (169 классов
рук по построению). max |net| = 10. Эвал НЕ древесный (дерево сдел слишком
велико): таблица эквити олл-ин 169×169 (MC 1M симуляций/матчап, фиксированный
сид, кэш) + CFR+-Нэш; шоудаун env платит по этой же таблице (модель игры
совпадает с якорем); гейт-метрика: NashConv в классовой игре (в bb) и
расхождение диапазонов (total variation) с точным Нэшем. Порог tier B
замораживается ПОСЛЕ расчёта точного Нэша и ДО первого обучения агента;
опубликованные чарты — вторичная sanity-проверка.

**Leduc (замороженное определение):** колода 6 (ранги J/Q/K × 2 масти), по одной
карте в руку, анте 1/1. Действия K=3: 0=фолд (легален ТОЛЬКО против ставки —
открытый фолд вырезен как доминируемый), 1=чек/колл (всегда легален), 2=бет/рейз
(легален при < 2 повышениях в раунде). Раунд 1: ставка 2, начинает игрок 0; после
раунда 1 открывается публичная карта (chance-узел посреди эпизода); раунд 2:
ставка 4, начинает игрок 1. Шоудаун: пара с бордом бьёт старшую карту, при равенстве
силы — сплит. Наблюдение кодирует РАНГИ (масти исключены конструктивно —
S4-канонизация по построению, как в §3, но сильнее: в Leduc сила руки зависит
только от рангов). Бухгалтерия ставок: каждое агрессивное действие добавляет
ФЛЭТ-ставку раунда (2 / 4) инвестору; колл не добавляет; при чередовании
инвестиции к концу раунда равны. Макс |net| = 7 (reward_scale выводится
анализатором). Ревизия до первого обучения: ранее здесь ошибочно значилось 13.
