[package]
name = "kalay_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "kalay_rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
rayon = "1.12"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
