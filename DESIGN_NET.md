# DESIGN_NET v2 — ЕДИНЫЙ пайплайн HU+ (по директиве владельца, до кода HUNL-toy)

ПРИНЦИП: **один пайплайн, одна реализация**. Все ступени лесенки от HUNL-toy и выше
идут ровно так, как пойдёт холдем на серьёзном обучении. Никаких эвристических
костылей, никакого брутфорса, **никаких бакетов карточных — фоллбек запрещён**.
Минимальные допущения только для безкарточных игр (RPS: только [META] и токены действий).

## 1. Лента токенов раздачи (append-only по улицам)

```
X = [ META | h1 h2 | b1 b2 b3 | b4 | b5 | act1 act2 ... ]     # bi появляются своей улицей
```

- **Карты**: одна таблица `nn.Embedding(52, D)`, токен = S4-канонизированная карта
  (наш hand-locked канонизатор — сильнее наивной сортировки: масти схлопнуты с
  сохранением флеш-дро, префикс-стабильность доказана тестами).
- **Рука = 2 ОТДЕЛЬНЫХ токена** (не склеивать): внимание само связывает каждую карту
  с бордом (топ-пара/кикер/дро — разные головы). Симметрия руки — каноническая
  сортировка + **один и тот же позиционный индекс обоим токенам руки** (порядок
  внутри руки не имеет веса).
- **Борд**: флоп-тройка канонически сортируется (борд неупорядочен), тёрн/ривер
  дописываются в порядке появления. Тип токена (`Hero|Board`) + улица появления
  (`PF/F/T/R`) — аддитивные эмбеддинги.
- **[META]-токен**: `E_pos[каноническое место относительно кнопки] + MLP_meta(v_table)`,
  `v_table = [eff_stack_bb, pot_bb, n_active]`. Расширяемость без ломки архитектуры:
  статы оппонентов, тип игры (холдем/пло).

## 2. Действия: числа через MLP, никаких «бет-токенов-дискреток»

Игрок не делает «бет-токен» — он въебывает 28.47 BB. Каждое действие — токен:

```
e_act = E_type[Fold|Check/Call|Bet/Raise] + E_pos[кто действовал] + E_round[улица] + e_num
e_num = MLP_num( γ(v_raw) ),   γ — Fourier/NeRF-проекция (k частот, sin+cos)
v_raw = [ bet/(pot+ε), bet/(stack+ε), log(1 + pot/(eff_stack+ε)), to_call/(pot+ε) ]
```

- **Инвариантность к фишкам/блайнундам**: сеть видит ТОЛЬКО отношения, никогда
  абсолютные значения (10000 vs 200 фишек — один и тот же спот).
- Fourier-проекция различает микро-сайзинги (низкие частоты — грубая шкала, высокие —
  тонкая), `Linear(2k·|v_raw|, D_num)`.
- Вход ставок непрерывен и дифференцируем целиком — чистый градиентный поток.

## 3. Внимание: block-causal (уличная каузальность) — объединяет оба твоих аргумента

- **Твоё «за каузальность»**: один forward() на всю раздачу выдаёт валидные предсказания
  сразу во ВСЕХ точках решений, градиенты параллельно по длине — решено.
- **Твоё «за bidirectional»**: карты одной улицы существуют одновременно, h1 не
  «предшествует» h2, флоп не должен не видеть свою третью карту — решено:
  **внутри уличного блока внимание полное, между улицами — только назад**
  (`mask[i,j] = street(j) ≤ street(i)`).
- **Readout** — на токенах действий (каждая точка решения): hidden action-токена
  после слоёв и есть «резюм» всего прошедшего к моменту решения; отдельный [CLS]
  не нужен. Позиции readout стабильны при дописывании будущих улиц →
  **append-only в точках считывания** = KV-кэш на инференсе (инвариант — tier A-тест).
- Оптимизатор/движок не меняются: R-NaD + NeuRD уже реализованы ровно по формуле
  `Ã(s,a) = Q − τ·log(π/π_ref)` с вложенными циклами якоря (nested loops).

## 4. Выход: сетка из K действий + маска легальности (выход, не вход!)

Непрерывную ставку регрессировать нельзя (мультимодальность: 50% чек / 50% овербет —
среднее бессмысленно) — выход квантован на K≈12: `[Fold, Check/Call, Min-Raise,
33%, 50%, 75%, 100%, 125%, 150%, 200%, 300% пот, All-in]` + бинарная маска
`m ∈ {0,1}^K`, нелегальным логит −1e9. Логика легальности — из легаси Rust
(`to_call / min_raise_to / max_raise_to`, лестница с гарантией 0% невалидных ходов) —
портируется в kalay_rs. Сетка якорится к границам легальности (min-raise / all-in),
не к абсолютным фишкам.

## 5. Батч/обучение

TrajectoryBatch расширяется: `tokens [T,B,L]`, `pad [T,B,L]`, `street_id [T,B,L]`,
`v_num [T,B,L,|v_raw|]` (для Fourier-проекции), readout-индексы. Движок (V-trace,
NeuRD, якоря, контракты) — без изменений: ему нужны logits [T,B,K] и V [T,B].
Паддинг — маской внимания; в одном батче смешиваются раздачи всех улиц.

## 6. Статус и очередь

Сделано: S4-канонизация (гарантии), движок R-NaD/NeuRD (двойная верификация),
легаси-логика маски найдена и оценена (`state.rs:1007+`, `ladder.rs` — к порту).
Доделать: v2-скелет трансформера (Fourier-голова, META, block-causal, multi-readout)
+ tier A → HUNL-toy определение и порог (пререгистрация) → словарь/пайплайн в
адаптере → интеграция → гейт. Прошлые MLP-ступени — история валидации RL-ядра;
всё вперёд — только этот пайплайн.

Критика: сюда; ревизии — явно, с версиями.
